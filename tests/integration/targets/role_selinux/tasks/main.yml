---
# tests/integration/targets/role_selinux/tasks/main.yml

################################################################################
# This is a test role, specifically designed for testing. It should not be     #
# used as a template for proper role development.                              #
################################################################################

- name: "Import selinux Role"
  ansible.builtin.import_role:
    name: "whiletruedoio.general.selinux"
  vars:
    selinux_checks_enabled: true
...
